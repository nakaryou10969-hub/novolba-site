# 作業ログ

## 2026年5月18日 — ドメイン移行・リダイレクト設定

### 目的
WordPress（EC2）で運用中の `novolba.com` を、Next.js + S3 + CloudFront 構成の新サイトに移行する。

---

### 実施内容

#### 1. ネームサーバーの移管
- 元のドメイン管理会社（別AWSアカウント）からRoute 53へネームサーバーを移管
- 移管完了後、`nslookup -type=NS novolba.com` で以下のRoute 53ネームサーバーへの切り替えを確認
  - `ns-253.awsdns-31.com`
  - `ns-939.awsdns-53.net`
  - `ns-1910.awsdns-46.co.uk`
  - `ns-1271.awsdns-30.org`

#### 2. ACM証明書の発行
- リージョン：`us-east-1`（バージニア北部）※CloudFront必須
- ドメイン：`novolba.com`、`www.novolba.com`
- 検証方法：DNS検証
- Route 53で検証用CNAMEレコードを自動作成し、証明書を発行済み
- ARN：`arn:aws:acm:us-east-1:305678528731:certificate/bda4f609-333e-44db-8aab-0a59ba61acf7`

#### 3. CloudFrontへの独自ドメイン追加（未完了・保留中）
- ディストリビューション：`E16AYFZ46ZVV6E`（`d1nqp9gphncdvt.cloudfront.net`）
- `novolba.com` と `www.novolba.com` を追加しようとしたが以下のエラーが発生：
  ```
  One or more of the CNAMEs you provided are already associated with a different resource.
  ```
- 原因：別AWSアカウントのRoute 53ホストゾーンにAレコード（EC2のIP）が残っている
- 対応：サーバー管理者に別アカウントのAレコード削除を依頼中

#### 4. Route 53のAレコード削除
- 自アカウントのRoute 53ホストゾーン `novolba.com` にあった以下のAレコードを削除
  - `novolba.com` → `d1nqp9gphncdvt.cloudfront.net`（Alias）
  - `www.novolba.com` → `d1nqp9gphncdvt.cloudfront.net`（Alias）
- 現在のレコード構成：NS、SOA、ACM検証用CNAME×2のみ

#### 5. /media/* → /with/* リダイレクト設定（方針変更により廃止予定）

##### 背景
- 旧WordPress URL：`novolba.com/media/記事スラッグ/`
- 新Next.js URL：`novolba.com/with/microCMSのid/`
- URLが変わるため、旧URLへのアクセスを新URLに301リダイレクトする必要があった

##### 対応手順
1. `scripts/generate_redirect_map.py` を作成・実行
   - microCMSの `with` エンドポイントから全203件のIDとslugを取得
   - 176件のリダイレクトマッピングを生成
   - 出力ファイル：`scripts/redirect_map.json`、`scripts/cloudfront_function.js`

2. CloudFront Functionsへの実装を試みたが **FunctionSizeLimitExceeded**（10KB超）でエラー

3. Lambda@Edgeに切り替え
   - リージョン：`us-east-1`
   - 関数名：`novolba-media-redirect`
   - ランタイム：`Node.js 22.x`
   - ファイル形式：`index.mjs`（ESモジュール）
   - コード：`scripts/lambda_edge_redirect_esm.js`（18KB）

4. IAMロールの設定
   - ロール名：`novolba-media-redirect-role-sjaw5lm0`
   - 信頼ポリシーに `edgelambda.amazonaws.com` を追加
   - `AWSLambdaBasicExecutionRole` ポリシーをアタッチ

5. CloudFrontトリガーの試行錯誤
   - オリジンリクエストで設定 → S3がキャッシュを先に返すためLambdaが動作しないことが判明
   - ビューワーリクエストに変更 → `add-index-html`（CloudFront Function）と競合
   - `add-index-html` を削除しLambda@Edgeにindex.html付与処理を統合（バージョン`:4`）
   - ビューワーリクエストで再設定したが、依然としてTOPページが返る問題が解消されず

6. **結論：リダイレクト方式を断念**
   - CloudFrontの動作制約により安定した実装が困難と判断
   - 現在のビヘイビア設定：ビューワーリクエスト = Lambda@Edge（`novolba-media-redirect:4`）

#### 6. microCMS重複記事の整理

##### 背景
- 移行スクリプトを2回実行したことで、同じスラッグを持つ記事が22件重複していた
- リダイレクトマップが古いIDを参照していたため、存在しないページに飛んでいた

##### 対応手順
1. `scripts/fix_duplicates.py` を作成・実行（ドライラン）
   - 重複22件を検出、古い方を削除・新しい方を残す方針を確認
2. `--execute` オプションで実行
   - APIキーの権限不足により削除は失敗（`DELETE is forbidden`）
   - リダイレクトマップの更新（22件）は成功
3. microCMS管理画面にて重複記事22件を手動削除（完了）
4. `scripts/gen_esm_v2.py` でLambdaコードを再生成・デプロイ

---

### 今後の方針（方針変更）

リダイレクト方式を断念し、**URLを旧サイトと完全一致させる方式**に変更する。

#### 必要な作業

| ステップ | 担当 | 状態 |
|---|---|---|
| microCMSの各記事にスラッグを手動設定 | 担当者 | ⏳ 未着手 |
| Next.jsのルーティングを `/media/[slug]/` に変更 | Kiro | ⏳ スラッグ設定完了後に実施 |
| GitHub Actionsで再ビルド・S3デプロイ | 自動 | ⏳ 上記完了後 |
| サーバー管理者のAレコード削除 | サーバー管理者 | ⏳ 依頼中 |
| CloudFrontに独自ドメイン追加 | 担当者 | ⏳ Aレコード削除後 |
| Route 53にAliasレコード作成 | 担当者 | ⏳ 上記完了後 |

#### ルーティング変更の概要（Kiroが実施）
- `app/with/[id]/page.tsx` → `app/media/[slug]/page.tsx` に変更
- `generateStaticParams` でIDではなくスラッグを使ってページ生成
- microCMSのslugフィールドが設定済みの記事のみページが生成される

---

### 現在の状態

| 項目 | 状態 |
|---|---|
| ネームサーバー移管 | ✅ 完了 |
| ACM証明書発行 | ✅ 完了 |
| microCMS重複記事整理 | ✅ 完了（22件削除） |
| Lambda@Edge設定 | ✅ デプロイ済み（ビューワーリクエスト） |
| CloudFrontへの独自ドメイン追加 | ⏳ 保留中（別アカウントのAレコード削除待ち） |
| Route 53 Aレコード作成 | ⏳ 保留中 |
| microCMSスラッグ手動設定 | ⏳ 未着手 |
| Next.jsルーティング変更 | ⏳ スラッグ設定完了後に実施 |

---

### 関連ファイル

| ファイル | 説明 |
|---|---|
| `scripts/generate_redirect_map.py` | microCMSからリダイレクトマップを生成するスクリプト |
| `scripts/fix_duplicates.py` | 重複記事を検出・整理するスクリプト |
| `scripts/gen_esm_v2.py` | Lambda@Edge用コード生成スクリプト（最新版） |
| `scripts/redirect_map.json` | slug → microCMS ID のマッピング（176件・更新済み） |
| `scripts/cloudfront_function.js` | CloudFront Functions用コード（サイズ超過で未使用） |
| `scripts/lambda_edge_redirect.js` | Lambda@Edge用コード（CommonJS形式・未使用） |
| `scripts/lambda_edge_redirect_esm.js` | Lambda@Edge用コード（ESM形式・現在デプロイ中） |

---

## 2026年5月20日 — UIコンテンツ改修

### 実施内容

#### 1. メタデータ（検索タイトル）の整備

- `app/layout.tsx` の `title.default` を `"NovolBa | 挑戦するスタートアップの昇る場"` に統一
- `title.template` を `"%s | NovolBa"` に設定し、各ページタイトルが `"ページ名 | NovolBa"` 形式で表示されるよう整備
- `siteName`・OGP・Twitter Card のブランド表記もすべて `"NovolBa"` に統一
- `keywords` に `"NovolBa"` を追加

#### 2. BASIC OFFICE 詳細ページの改修（`app/service/basic-office/page.tsx`）

- **870万円の費用内訳ブロック**（コード実装の表）を削除
- **入居月コスト・退去月コスト の比較表**（コード実装）を `chart.png` 画像に差し替え
- `public/chart.png` を Git に追加・プッシュ（未追跡だったため表示されていなかった問題を修正）

#### 3. MEDIAページの改修（`app/media/page.tsx`）

##### 最新記事スライダーの追加
- 新ファイル `app/media/LatestSlider.tsx`（Client Component）を作成
- `with` API から最新10件を取得し、3件ずつ左右矢印＋ドットインジケーターで切り替えるスライダーを実装
- 挿入位置：検索欄セクションの直後、インタビューカテゴリの直前

##### カテゴリー表示順の変更
- `CATEGORY_ORDER` を以下の順に変更：
  1. インタビュー
  2. イベント（← 移動）
  3. レポート（← 移動）
  4. 速報インタビュー
  5. 対談
  6. スタートアップ昇る場
  7. コラム

##### WITHとは？ボタンの追加
- 「すべての記事を見る」ボタンの直上に「WITHとは？」ボタンを設置
- リンク先：`/media/about`

#### 4. WITHとは？ページの新規作成（`app/media/about/page.tsx`）

- 参考：`https://novolba.com/media/about/`
- `with.png` をメインビジュアルとして使用
- 掲載内容：
  - キャッチコピー「共に 夢を魅せ / 共に 社会を変える / WITH STARTUPS」
  - メディア説明文
  - 運営会社情報（会社名・代表者・事業内容・WEB）
  - SNSリンク（Facebook・X・Instagram・YouTube）
  - 「メディアトップへ戻る」ボタン
- `public/with.png` を Git に追加・プッシュ（未追跡だったため表示されていなかった問題を修正）

---

## 2026年5月27日 — 本番移行完了

### 実施内容

#### 1. CNAME衝突の解消
- ボンレックス管理AWSアカウント（末尾: 026509）のCloudFrontディストリビューション（末尾: I3ETYB6）に `novolba.com` / `www.novolba.com` が代替ドメイン名として登録されていたことを `list-conflicting-aliases` APIで特定
- ボンレックスに削除依頼 → 対応完了

#### 2. CloudFront設定完了
- ディストリビューション `E16AYFZ46ZVV6E` にACM証明書（ARN末尾: acf7）を設定
- 代替ドメイン名 `novolba.com` / `www.novolba.com` を追加 → Deployed

#### 3. Route53 Aliasレコード作成
- `novolba.com` → CloudFront `d1nqp9gphncdvt.cloudfront.net`（Alias Aレコード）
- `www.novolba.com` → CloudFront `d1nqp9gphncdvt.cloudfront.net`（Alias Aレコード）

#### 4. NS委任切替
- ドメインレジストラ：Amazon Registrar, Inc.（ボンレックスAWSアカウント管理）
- ボンレックスがRoute53「登録済みドメイン」からNSを新側（ns-253系）に変更
- TLD委任切替完了

### 動作確認結果

| 確認項目 | 結果 |
|---|---|
| `www.novolba.com` HTTPS | ✅ CloudFront経由（`X-Amz-Cf-Id` 確認） |
| 新サイト（Next.js）表示 | ✅ シークレットウィンドウで確認 |
| メール受信（M365） | ✅ 外部→社内アドレス受信確認 |

### 現在の状態

| 項目 | 状態 |
|---|---|
| サイト移行 | ✅ 完了 |
| メール疎通 | ✅ 確認済み |
| 旧EC2停止 | ⏳ 1週間モニタリング後に検討 |

---

### 現在の状態（更新）

| 項目 | 状態 |
|---|---|
| メタデータ整備（全ページ） | ✅ 完了 |
| BASIC OFFICEページ改修 | ✅ 完了 |
| MEDIAページ 最新記事スライダー追加 | ✅ 完了 |
| MEDIAページ カテゴリー順変更 | ✅ 完了 |
| WITHとは？ページ新規作成 | ✅ 完了 |
| chart.png / with.png のGit追加 | ✅ 完了 |

---

## 2026年5月27日（続き）— mediaページ・URLルーティング・DNS問題

### 実施内容

#### 1. mediaページのURLを `/with/` から `/media/` に変更

**背景**：記事詳細ページのURLが `/with/[id]` になっており、旧サイトの `/media/` と異なっていた。ユーザーからの要望で `/media/[id]` に統一。

**実施内容**：
- `app/media/[id]/page.tsx` 新規作成（記事詳細ページ）
- `app/media/all/page.tsx` 新規作成（全記事一覧）
- `app/media/category/[name]/page.tsx` 新規作成（カテゴリ別一覧）
- `app/media/constants.ts` 新規作成（日本語定数をTurbopackバグ回避のため分離）
- `app/media/page.tsx`、`LatestSlider.tsx`、`MediaSearch.tsx` のリンクを `/with/` → `/media/` に変更
- GitHub Actionsでビルド・デプロイ完了

**トラブル**：ローカルビルドでTurbopackが `app/media/page.tsx` の440バイト目でUTF-8エラーを出し続けた。ローカルのTurbopackバグと判断し、GitHub Actions（Linux環境）でビルドしたところ正常に完了。

#### 2. wwwリダイレクト実装と競合問題

**背景**：`www.novolba.com` → `novolba.com` への301リダイレクトが未設定で、検索エンジンが `www.novolba.com` をインデックスしていた。

**実施内容**：
- CloudFront Functions `www-to-apex-redirect` を作成・Publish
- CloudFrontのビューワーリクエストに紐付け → Deployed

**問題発覚**：CloudFront FunctionsとLambda@Edgeを同じビューワーリクエストに設定すると競合し、Lambda@Edgeが動作しなくなる（CloudFrontの仕様）。その結果 `/media/` などのページで `index.html` 付与処理が動かず503が発生。

**対処**：CloudFront Functionsを切り離し、Lambda@Edge（バージョン4）のみに戻した。wwwリダイレクトは一時的に無効化。

#### 3. `OriginReadTimeout` 不正フィールド問題

**背景**：CloudFrontのオリジン設定に `OriginReadTimeout: 30` という不正なフィールドが混入していた（AWS CLIでの設定変更時に混入したと推定）。これによりCloudFrontがレガシーモードで動作し、S3ではなく旧EC2にリクエストが流れていた。

**対処**：CloudShellでPythonスクリプトを使い `OriginReadTimeout` を除去してCloudFrontを更新。

#### 4. `/media/` が旧サイトを表示する問題（DNS起因）

**現象**：`/media/` にアクセスすると旧WordPressが表示される。`server: Apache/2.4.43` が返り、`x-amz-cf-id` ヘッダーがない。

**原因**：
- 新Route53のAレコードは正しくCloudFrontを向いている（`3.173.197.x`）
- しかし旧EC2のRoute53ホストゾーンにAレコード（`35.76.225.240` / `13.192.124.191` / `57.181.19.115`）が残っている
- 一般リゾルバが旧EC2のIPをキャッシュし続けているため、CloudFrontに到達できない

**対処**：ボンレックスに旧Route53ホストゾーンのAレコード削除を依頼中。

**依頼内容**：
```
旧Route53ホストゾーン（novolba.com）内の以下のAレコードを削除：
- novolba.com → 35.76.225.240 / 13.192.124.191 / 57.181.19.115
- www.novolba.com → 同上
※NSレコード・MXレコード・その他は削除不要
```

---

### 現在の状態

| 項目 | 状態 |
|---|---|
| サイト移行（TOP・その他ページ） | ✅ 完了（DNS解決後） |
| メール疎通 | ✅ 確認済み |
| mediaページ（/media/） | ⏳ ボンレックスのAレコード削除待ち |
| wwwリダイレクト | ⏳ Lambda@Edgeへの統合が必要（現在無効） |
| mediaページURLの /with/ → /media/ 変更 | ✅ デプロイ済み |
| TOPページ以外が404になる問題 | ✅ 解消（DNS伝播完了後に自動解消。hostsファイルで動作確認済み） |
| ヘッダーのNEWSリンクが機能しない問題 | ✅ 解消（同上） |
| 旧EC2停止 | ⏳ Aレコード削除・1週間モニタリング後に検討 |

---

### 次のアクション

1. **ボンレックスからAレコード削除完了の連絡を受けたら**、シークレットウィンドウで `https://novolba.com/media/` を確認
2. **wwwリダイレクト**：Lambda@Edge（バージョン4）のコードにwwwリダイレクト処理を統合した新バージョンをデプロイする（CloudFront FunctionsとLambda@Edgeの競合を避けるため）
3. **TOPページ以外が404になる問題**：TOPページ（`/`）は表示されるが、それ以外のページ（例：`/company/`、`/service/` 等）にアクセスすると404になる。S3の静的ホスティングでは `index.html` の付与処理が必要なため、Lambda@Edgeの `index.html` 付与処理が正常に動作していない可能性がある。
4. **ヘッダーのNewsリンクが機能しない問題**：ヘッダーの「NEWS」をクリックしても遷移しない。リンク先のパスが未実装またはルーティングが誤っている可能性がある。

---

## 【次のチャットへの引き継ぎメモ】

### 現在待機中のタスク

**ボンレックスからAレコード削除完了の連絡待ち。**

連絡が来たら以下を即座に実行：

```powershell
Invoke-WebRequest -Uri "https://novolba.com/media/" -UseBasicParsing -MaximumRedirection 0 -ErrorAction SilentlyContinue 2>&1 | Select-Object StatusCode, @{N='Server';E={$_.Headers.Server}}, @{N='CF';E={$_.Headers.'x-amz-cf-id'}}
```

- `Server: AmazonS3` または `x-amz-cf-id` が返れば → ✅ 新サイト切替完了
- `Server: Apache/2.4.43` が返れば → ⏳ まだキャッシュが残っている（数分待って再確認）

---

### 未解決の不具合

#### TOPページ以外が404になる問題（2026/05/27 調査済み）
- **現象**：`/service/` が404。`/company/`、`/news/` 等は200だが旧WordPressのコンテンツが返っている
- **実測エビデンス**：全ページで `Server: Apache/2.4.43`、`x-amz-cf-id` ヘッダーなし → CloudFrontに未到達
- **根本原因**：DNS未切替。ボンレックスの旧Route53ホストゾーンにAレコード（`35.76.225.240` 等）が残存しており、リクエストが旧EC2に流れている。`/service/` が404なのは旧WordPressにそのパスが存在しないため
- **対応方針**：ボンレックスのAレコード削除待ち。DNS切替後はLambda@Edgeの `index.html` 付与処理が全パスで正常動作するか別途確認が必要

#### ヘッダーのNEWSリンクが機能しない問題（2026/05/27 調査済み）
- **現象**：ヘッダーの「NEWS」をクリックしても遷移しない（または旧サイトのページが返る）
- **実測エビデンス**：`Header.tsx` の href は `/news`（末尾スラッシュなし）。`next.config.ts` の `trailingSlash: true` により `/news/` にリダイレクトされるが、現在は旧EC2が応答しているため旧WordPressの挙動に依存している
- **根本原因**：同上（DNS未切替）。新サイトのNEWSページ（`app/news/page.tsx`）は実装済みで問題なし
- **対応方針**：DNS切替後に自動解消する見込み。切替後に動作確認を行う

---

### 完了後にやること（wwwリダイレクト復活）

`/media/` 問題が解決したら、wwwリダイレクトをLambda@Edgeに統合する。

**現状**：
- Lambda@Edge `novolba-media-redirect:4` がビューワーリクエストに設定済み
- バージョン4のコードにはwwwリダイレクト処理が**含まれていない**
- CloudFront Functions `www-to-apex-redirect` は切り離し済み（Lambda@Edgeと競合するため）

**やること**：
1. `scripts/lambda_edge_redirect_esm.js` のhandler先頭にwwwリダイレクト処理を追加（既に追加済みのコードあり）
2. CloudShellで新バージョンをデプロイ（`index.mjs` という名前でzip化してアップロード）
3. CloudFrontのビューワーリクエストを新バージョンに更新

**注意**：Lambda@Edgeのバージョン更新はCloudShellで行うこと（ローカルPCからのCLI操作は不正フィールド混入のリスクあり）。

---

### 重要な設定値

| 項目 | 値 |
|---|---|
| CloudFront ID | `E16AYFZ46ZVV6E` |
| Lambda関数名 | `novolba-media-redirect` |
| 現在のLambdaバージョン | `:4` |
| S3バケット | `novolba-com-contents` |
| Route53ホストゾーンID | `Z0623222199M3XVL9LLSY` |

---

## 2026年5月27日（続き）— DNS伝播・動作確認

### 実施内容

#### 1. ボンレックスのAレコード削除完了

ボンレックスより旧Route53ホストゾーンのAレコード削除完了の連絡を受けた。

削除されたレコード：
- `novolba.com` → `35.76.225.240` / `13.192.124.191` / `57.181.19.115`
- `www.novolba.com` → 同上

#### 2. CloudFront経由での動作確認

CloudFrontのIP（`3.173.197.78`）に `--resolve` オプションで直接アクセスし、全ページの正常動作を確認。

```
/          → HTTP 200 | AmazonS3 | CF:YES
/company/  → HTTP 200 | AmazonS3 | CF:YES
/service/  → HTTP 200 | AmazonS3 | CF:YES
/news/     → HTTP 200 | AmazonS3 | CF:YES
/media/    → HTTP 200 | AmazonS3 | CF:YES
/message/  → HTTP 200 | AmazonS3 | CF:YES
/faq/      → HTTP 200 | AmazonS3 | CF:YES
/inquiry/  → HTTP 200 | AmazonS3 | CF:YES
```

サイト自体は完全に正常。問題はDNS伝播待ちのみ。

#### 3. DNS伝播状況

Aレコード削除後、Google DNS・Cloudflare DNSともにIPが返らない状態が続いた（旧レコードのネガティブキャッシュ残存）。

Route53のSOA確認結果：
- `default TTL = 86400`（1日）
- 最大24時間の伝播待ちが必要な状態

#### 4. hostsファイルによる一時的な動作確認

DNS伝播を待たずに確認するため、管理者PowerShellでhostsファイルに以下を追記：

```
3.173.197.78  novolba.com
3.173.197.78  www.novolba.com
```

Chromeのシークレットウィンドウで全ページの表示を確認 → **完璧に表示されることを確認**。

#### 5. DNS伝播完了後の作業（未実施）

Google DNS（8.8.8.8）でIPが返ってきたら以下を実施：

1. hostsファイルから一時設定を削除（管理者PowerShellで実行）：
```powershell
(Get-Content "C:\Windows\System32\drivers\etc\hosts") | Where-Object { $_ -notmatch "novolba" } | Set-Content "C:\Windows\System32\drivers\etc\hosts" -Encoding ASCII
```

2. シークレットウィンドウで `https://novolba.com/company/` 等にアクセスして正常表示を確認

3. wwwリダイレクトの実装（Lambda@Edgeへの統合）

---

### 現在の状態

| 項目 | 状態 |
|---|---|
| サイト移行（全ページ） | ✅ 完了（CloudFront経由で全ページ正常動作確認済み） |
| メール疎通 | ✅ 確認済み |
| DNS伝播 | ⏳ 進行中（Google DNS・Cloudflare DNSでまだIPが返っていない） |
| hostsファイル一時設定 | ⚠️ 追記中（DNS伝播完了後に削除が必要） |
| mediaページ（/media/） | ✅ 正常動作確認済み |
| mediaページURLの /with/ → /media/ 変更 | ✅ デプロイ済み |
| wwwリダイレクト | ⏳ Lambda@Edgeへの統合が必要（現在無効） |
| 旧EC2停止 | ⏳ DNS伝播完了・1週間モニタリング後に検討 |

---

### DNS伝播確認コマンド

```powershell
nslookup novolba.com 8.8.8.8
```

以下のようにAddressesにIPが返ってきたら伝播完了：

```
名前:    novolba.com
Addresses:  3.173.197.78
            3.173.197.6
            3.173.197.40
            3.173.197.58
```

---

## 2026年5月28日 — DNS未伝播の根本原因判明・旧サイト確認

### 実施内容

#### 1. DNS未伝播の根本原因調査

昨日18時にボンレックスがAレコードを削除してから15時間経過してもDNS伝播しない原因を調査。

**TLDサーバーへの直接問い合わせで判明した構造：**

```
TLDサーバー（世界の権威）
  → ns-280.awsdns-35.com 系ホストゾーン（ボンレックスAWSアカウント）
    ※ Aレコードなし・NSレコードのみ（ns-253系を指している）
      → ns-253.awsdns-31.com 系ホストゾーン（自アカウント）
        ※ Aレコードあり → CloudFront
```

TLDが委任しているのはボンレックスのホストゾーン（ns-280系）だが、そこにAレコードがないためDNS解決が失敗していた。

**原因：** ドメインレジストラ（Amazon Registrar）がボンレックスのAWSアカウントで管理されており、NSの委任先がボンレックスのホストゾーンのまま。

#### 2. 応急処置と根本解決の方針

**応急処置（短期）：**
ボンレックスのホストゾーン（ns-280系）にCloudFrontへのAliasレコードを追加してもらう。

**根本解決（中期）：**
ドメイン `novolba.com` のレジストラ移管をボンレックスから自アカウントへ実施。

#### 3. ボンレックスへの依頼メール送付

以下2点を依頼するメールを送付：

1. **Aレコードの追加**（応急処置）
   - `novolba.com` → A Alias → `d1nqp9gphncdvt.cloudfront.net`（us-east-1）
   - `www.novolba.com` → A Alias → `d1nqp9gphncdvt.cloudfront.net`（us-east-1）
   - 対象ホストゾーン：ns-280.awsdns-35.com が権威サーバーのもの

2. **ドメイン移管用承認コード（Auth Code / EPP Code）の発行**

#### 4. hostsファイルによる動作確認（継続中）

DNS伝播待ちの間、hostsファイルで `3.173.197.78 novolba.com` を設定して新サイトを確認中。全ページ正常表示を確認済み。

#### 5. 旧WordPressサイトの確認

hostsを `35.76.225.240 novolba.com` に切り替えて旧サイトにアクセス。必要なコンテンツの確認・引き継ぎ作業を実施。作業完了後、CloudFront用（`3.173.197.78`）に戻した。

---

### 現在の状態

| 項目 | 状態 |
|---|---|
| サイト移行（全ページ） | ✅ 完了（hostsファイル経由で確認済み） |
| DNS伝播 | ⏳ ボンレックスのAレコード追加対応待ち |
| hostsファイル一時設定 | ⚠️ `3.173.197.78 novolba.com` 設定中（DNS解決後に削除が必要） |
| ドメイン移管 | ⏳ ボンレックスへ承認コード発行を依頼中 |
| wwwリダイレクト | ⏳ Lambda@Edgeへの統合が必要（現在無効） |
| 旧EC2停止 | ⏳ DNS伝播完了・1週間モニタリング後に検討 |

---

### ボンレックス対応完了後にやること

1. `nslookup novolba.com 8.8.8.8` でIPが返ることを確認
2. hostsファイルのnovolba行を削除（管理者PowerShellで）：
   ```powershell
   (Get-Content "C:\Windows\System32\drivers\etc\hosts") | Where-Object { $_ -notmatch "novolba" } | Set-Content "C:\Windows\System32\drivers\etc\hosts" -Encoding ASCII
   ```
3. シークレットウィンドウで全ページ正常表示を確認
4. ドメイン移管手続き（承認コード受領後）
5. wwwリダイレクトのLambda@Edge統合

---

## 2026年5月28日（続き）— DNS根本原因判明・打ち合わせ準備

### 実施内容

#### 1. DNS未伝播の根本原因（最終判明）

TLDサーバーへの直接問い合わせで以下の構造が判明：

```
TLDサーバー（世界の権威）
  → ボーンレックスのホストゾーン（ns-280系）← レジストラの委任先
      ├─ Aレコード：35.76.225.240（旧EC2）← 旧サイトに繋がる
      └─ NSレコード：ns-253系を指している（ボーンレックスが設定済み）
           ↓
      あなたのホストゾーン（ns-253系）
        ├─ Aレコード：3.173.197.78（CloudFront）✅
        └─ MXレコード：Microsoft 365 ✅
```

ボーンレックスがホストゾーン内のNSレコードを変更したが、レジストラ（登録済みドメイン）側のネームサーバー設定は `ns-280系` のままのため、世界のDNSはボーンレックスのホストゾーンで止まっている。

#### 2. 現在の状態（実測）

```powershell
# 確認コマンド
nslookup -type=A novolba.com 8.8.8.8
# → 35.76.225.240（旧EC2）が返る = 旧サイトが表示される状態
```

#### 3. 方針決定

- **STEP 1（Aレコード応急処置）はスキップ**
  - 旧サイトを緊急措置として継続表示する必要があるため急がない
  - STEP 2完了でボーンレックスのホストゾーン自体を経由しなくなるため不要
- **STEP 2（レジストラのNS変更）を最優先で依頼**

#### 4. ボーンレックスへの依頼メール送付

レジストラのネームサーバー変更を依頼するメールを送付。

**依頼内容：**
AWSコンソール → Route53 → **「登録済みドメイン」**（ホストゾーンではない）→ `novolba.com` のネームサーバーを以下に変更：

```
ns-253.awsdns-31.com
ns-939.awsdns-53.net
ns-1910.awsdns-46.co.uk
ns-1271.awsdns-30.org
```

#### 5. 共有ドキュメント作成

`現状共有.md` を作成・更新。打ち合わせ用に以下を記載：
- 現在の状態（旧サイトが表示されている）
- DNS経路の図解
- ボーンレックス側の作業経緯
- 完全移行ロードマップ（STEP 1〜6）
- ドメイン移管手順・承認コード発行手順

---

### 現在の状態

| 項目 | 状態 |
|---|---|
| サイト移行（全ページ） | ✅ CloudFront・S3は正常動作確認済み |
| 一般公開 | ❌ 旧サイト（WordPress）が表示されている |
| DNS根本原因 | ✅ 判明（レジストラのNS設定がボーンレックスのホストゾーンを向いている） |
| ボーンレックスへの依頼 | ⏳ レジストラのNS変更を依頼中 |
| hostsファイル | ✅ 削除済み（一時設定なし） |
| メール（M365） | ✅ 正常動作中 |
| ドメイン移管 | ⏳ NS変更完了・安定確認後に実施予定 |
| wwwリダイレクト | ⏳ NS変更完了後に実施 |
| 旧EC2停止 | ⏳ 移管完了・1週間モニタリング後 |

---

## 【次のチャットへの引き継ぎメモ】

### 最優先で確認すること

**ボーンレックスからレジストラのNS変更完了の連絡が来たら、以下を即座に実行：**

```powershell
# DNS伝播確認
nslookup -type=NS novolba.com a.gtld-servers.net
# → ns-253.awsdns-31.com 系が返ってきたら成功

nslookup -type=A novolba.com 8.8.8.8
# → 3.173.197.78 が返ってきたら新サイトに繋がっている
```

ブラウザのシークレットウィンドウで `https://novolba.com/` を確認して新サイトが表示されればOK。

---

### 完了後にやること（順番通りに実施）

**STEP 3：hostsファイル削除（不要になった場合）**
```powershell
# 管理者PowerShellで実行
(Get-Content "C:\Windows\System32\drivers\etc\hosts") | Where-Object { $_ -notmatch "novolba" } | Set-Content "C:\Windows\System32\drivers\etc\hosts" -Encoding ASCII
```

**STEP 4：ドメイン移管**
- ボーンレックスから承認コード（Auth Code）を受け取る
- AWSコンソール → Route53 → 「登録済みドメイン」→「ドメインの移管」→ `novolba.com` を入力 → 承認コードを入力 → 申請
- ボーンレックス側のメールに確認メールが届くので承認してもらう（3〜5日）

**STEP 5：wwwリダイレクト実装（Kiroが実施）**
- Lambda@Edge（`novolba-media-redirect`）の新バージョンをCloudShellでデプロイ
- `scripts/lambda_edge_redirect_esm.js` のhandler先頭にwwwリダイレクト処理を追加（コードは既に準備済み）
- CloudShellで zip化してアップロード → CloudFrontのビューワーリクエストを新バージョンに更新
- ⚠️ ローカルPCからのCLI操作は不正フィールド混入リスクあり。必ずCloudShellで実施

**STEP 6：旧EC2停止**
- STEP 2完了から1週間モニタリング後にボーンレックスに依頼

---

### 重要な設定値

| 項目 | 値 |
|---|---|
| CloudFront ID | `E16AYFZ46ZVV6E` |
| CloudFrontドメイン | `d1nqp9gphncdvt.cloudfront.net` |
| CloudFrontのIP | `3.173.197.78`（他3件） |
| S3バケット | `novolba-com-contents` |
| Lambda関数名 | `novolba-media-redirect` |
| 現在のLambdaバージョン | `:4` |
| Route53ホストゾーンID（自アカウント） | `Z0623222199M3XVL9LLSY` |
| 自アカウントのNS | `ns-253.awsdns-31.com` 系（4つ） |
| ボーンレックスのNS | `ns-280.awsdns-35.com` 系（4つ）← 現在TLDが参照中 |
| ACM証明書ARN | `arn:aws:acm:us-east-1:305678528731:certificate/bda4f609-333e-44db-8aab-0a59ba61acf7` |
| microCMS サービスドメイン | `miu4stli4b` |

### 関連ファイル

| ファイル | 説明 |
|---|---|
| `novolba-site/現状共有.md` | 打ち合わせ用共有ドキュメント（DNS構造・ロードマップ・手順） |
| `scripts/lambda_edge_redirect_esm.js` | Lambda@Edge用コード（wwwリダイレクト処理追加済み・未デプロイ） |
| `scripts/index.mjs` | 同上（CloudShellデプロイ用） |

---

## 2026年6月1日 — DNS移行完了・wwwリダイレクト実装・URLルーティング変更

### 実施内容

#### 1. レジストラのネームサーバー変更（STEP 1完了）

ボーンレックスがAWSコンソール「登録済みドメイン」のネームサーバーを以下に変更：

```
ns-253.awsdns-31.com
ns-939.awsdns-53.net
ns-1910.awsdns-46.co.uk
ns-1271.awsdns-30.org
```

変更後、TLDサーバーおよびGoogle DNS（8.8.8.8）でCloudFrontのIP（`3.173.197.x`）が返ることを確認。DNS伝播完了。

#### 2. 新サイト全体公開確認（STEP 2完了）

シークレットウィンドウで全ページ正常表示を確認：
- `https://novolba.com/` ✅
- `https://novolba.com/company/` ✅
- `https://novolba.com/media/` ✅
- `https://www.novolba.com/` ✅

#### 3. wwwリダイレクト実装（STEP 5完了）

Lambda@Edge（`novolba-media-redirect`）に `www.novolba.com` → `novolba.com` への301リダイレクト処理を統合した新バージョンをデプロイ。

- CloudShellでindex.mjsをアップロード → `publish-version` でバージョン`:7`を発行
- CloudFrontのビューワーリクエストを`:4` → `:7`に更新（Deployed確認済み）
- 動作確認：`www.novolba.com` → `301 → https://novolba.com/` ✅

#### 4. 記事URLのルーティング変更（/media/[id] → /media/[slug]）

記事詳細ページのURLをmicroCMSのIDベースからスラッグベースに変更。

**変更ファイル：**
- `app/media/[id]/page.tsx` — `generateStaticParams` でslugを使用、記事取得をslug優先・idフォールバックに変更
- `app/media/page.tsx` — ArticleCardのリンクを `slug ?? id` に変更
- `app/media/all/page.tsx` — 記事リンクを `slug ?? id` に変更
- `app/media/category/[name]/page.tsx` — 記事リンクを `slug ?? id` に変更
- `app/media/LatestSlider.tsx` — 記事リンクを `slug ?? id` に変更
- `app/media/MediaSearch.tsx` — Article型にslugフィールド追加、リンクを `slug ?? id` に変更

**方針：** slugが設定されている記事はslugをURL、未設定の記事はidをURLとして使用（後方互換性を維持）。

---

### 現在の状態

| 項目 | 状態 |
|---|---|
| DNS移行（レジストラNS変更） | ✅ 完了 |
| 新サイト全体公開 | ✅ 完了 |
| wwwリダイレクト | ✅ Lambda@Edge `:7` でデプロイ済み |
| 記事URL（slug化） | ✅ デプロイ済み |
| ドメイン移管（STEP 3） | ⏳ ボーンレックスから承認コード受領後に実施 |
| 旧EC2停止（STEP 4） | ⏳ 移行完了から1週間モニタリング後にボーンレックスへ依頼 |

### 重要な設定値（更新）

| 項目 | 値 |
|---|---|
| CloudFront ID | `E16AYFZ46ZVV6E` |
| Lambda関数名 | `novolba-media-redirect` |
| 現在のLambdaバージョン | `:7` |
| S3バケット | `novolba-com-contents` |
| Route53ホストゾーンID | `Z0623222199M3XVL9LLSY` |
